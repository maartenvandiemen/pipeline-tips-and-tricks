trigger: none

pool:
  vmImage: ubuntu-latest

variables:
  buildConfiguration: release
  buildConfigurationExtra: debug

steps:
- checkout: none
- template: template.yml
  parameters:
    buildConfiguration: $(buildConfigurationExtra)
